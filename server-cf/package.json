{
  "name": "suisui-cf",
  "version": "2.13.2",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --local",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "db:create": "wrangler d1 create suisui",
    "db:migrate:local": "wrangler d1 migrations apply suisui --local",
    "db:migrate:remote": "wrangler d1 migrations apply suisui --remote"
  },
  "dependencies": {
    "hono": "^4.6.14"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260820.1",
    "typescript": "~5.7.3",
    "vitest": "^4.1.8",
    "wrangler": "^4.0.0"
  }
}
